package com.compassuol.springcoredemo;

// Criando nossa interface com um método
// Em que nesse caso, toda classe que usar nossa interface (implements)
// Terá que implementar nossa função abaixo
public interface Coach {

    // Função que deverá ser implementada pela classe que usar o implements da nossa interface
    String getDailyWorkout();

}
